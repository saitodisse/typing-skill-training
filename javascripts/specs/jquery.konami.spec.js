describe("konami keyup tests", function() {
  it("function $.fn.konami exists", function() {
    expect($.prototype.konami).toBeDefined();
    expect($.fn.konami).toBeDefined();
  });
  it("when initiated konami.enabled was triggered", function() {
    //expect($.fn.konami).toBeDefined();
  });
});
